# PoC 結果

| 項目 | 内容 |
| --- | --- |
| ステータス | 進行中（P5 完了 / P1・P2・P3・P4 未着手） |
| 対象 | [05-technology-selection.md §7](./05-technology-selection.md#7-決定前に検証すべきことpoc) |
| 最終更新 | 2026-09-05 |
| コード | [`poc/`](../../poc/README.md) |

## 1. 進捗

| # | 検証項目 | 状態 | 結果の要約 |
| --- | --- | --- | --- |
| P5 | 重みを `ArrayBuffer` として OPFS に保存・読み戻し | **完了** | 成立。ただし `persist()` が拒否された |
| P1 | Worker 内での推論と1枚あたりの実行時間 | 未着手 | — |
| P4 | WebGPU / WASM の速度差、COOP・COEP の要否 | 未着手 | — |
| P2 | ブラウザ内の転移学習が動くか | 未着手 | — |
| P3 | 検出ヘッドの差し替えによるクラス数変更 | 未着手 | — |

## 2. 測定環境

| 項目 | 値 |
| --- | --- |
| OS | Windows 11 |
| ブラウザ | Chrome（`chrome://newtab` から localhost を開いた通常セッション） |
| 配信 | `node poc/serve.mjs`（COOP / COEP なし） |
| 実行日 | 2026-09-05 |

**1環境での1回の測定であり、性能値は目安。** 判断に使うのは主に「成立するか否か」と桁の把握。

## 3. P5 — OPFS への保存・読み戻し

### 3.1 結果

**すべての項目で成立した。**[04-storage-design.md](./04-storage-design.md) の前提は維持できる。

| 項目 | 結果 |
| --- | --- |
| OPFS（`navigator.storage.getDirectory`） | 利用可 |
| `createSyncAccessHandle`（Worker） | 利用可 |
| `ArrayBuffer` の往復と整合性（SHA-256 で照合） | 5MB / 20MB / 60MB すべて一致 |
| ディレクトリ列挙 | 可能。ファイル名とサイズを取得できた |
| 削除後の使用量 | 0MB に戻った |

### 3.2 読み書き性能

| 方式 | サイズ | write | read |
| --- | --- | --- | --- |
| メインスレッド（`createWritable`） | 5MB | 19ms | 7ms |
| | 20MB | 49ms | 14ms |
| | 60MB | 84ms | 30ms |
| Worker（`createSyncAccessHandle`） | 5MB | 13ms | 6ms |
| | 20MB | 90ms | 12ms |
| | 60MB | 63ms | 35ms |

**[04 §9](./04-storage-design.md#9-容量の見積もり) で想定した「1世代20MB」のモデルは、読み書き100ms未満で扱える。** 容量見積もりの範囲では、ストレージI/Oがボトルネックになる兆候はない。

メインスレッドと Worker で有意な差は出なかった。**Worker を使う理由は性能ではなく、[03 の論点19](./03-system-architecture.md#実行スレッド構成論点19) で決めた実行スレッド構成に従うため**という位置づけで問題ない。

### 3.3 容量

`navigator.storage.estimate()` の `quota` は **10,240MB（10GB）** だった。

[04 §2.2](./04-storage-design.md#22-容量の上限) では「数百MB〜数GB程度の幅がある」と書いたが、この環境ではそれを上回った。ただしブラウザ・OS・ディスク空き容量に依存する点は変わらないため、**特定の容量を前提としない方針は維持する。**

### 3.4 重要な発見 — `persist()` が拒否された

```text
persisted (要求前): false
persist() の結果:   false
```

**[04 §2.1](./04-storage-design.md#21-データを捨てないを脅かす最大のリスク) で懸念した状況が、実際に再現した。**

この環境では永続化が許可されず、ブラウザによる自動削除の対象のままである。localhost かつサイトへの利用実績がない状態のため、本番配信で利用を重ねれば許可される可能性はあるが、**「拒否されうる」ではなく「実際に拒否される」ことが確認できた。**

論点26で決めた方針（起動時に要求し、拒否時は警告してエクスポートを促す。操作はブロックしない）は妥当だったが、次の点を補強する必要がある。

- 警告は「まれな異常系」ではなく**初期状態として起こりうるもの**として扱う。初回起動時に拒否されるのが通常だと考えて、UIの導線を設計する。
- エクスポート機能（方針17）の実装優先度は、想定よりさらに高い。MVP スコープ（推論 + 履歴）には含まれていないが、**学習機能を出す前には必須**と位置づける。

### 3.5 副次的に得られた情報

| 項目 | 値 | 関連 |
| --- | --- | --- |
| `crossOriginIsolated` | false | 論点34。COOP / COEP なしの既定状態 |
| `SharedArrayBuffer` | 利用不可 | 論点34。WASM マルチスレッドには COOP / COEP が要る |
| `navigator.gpu`（WebGPU） | **利用可** | 論点34。この環境では WebGPU が選択肢になる |

P4 で `--isolated` オプション付きの配信と比較すれば、COOP / COEP の要否を判断できる。

## 4. 論点への影響

| 論点 | 影響 |
| --- | --- |
| 25（実体の格納先） | **決定を裏付けた。** OPFS で成立し、フォールバックは当面不要と見込める |
| 26（`persist()` の扱い） | **決定は妥当だが、拒否が初期状態として起こる前提で運用を考える必要がある** |
| 27（不整合の回復） | ディレクトリ列挙が可能なため、起動時の孤児GCは実装できる |
| 34（実行バックエンド） | WebGPU が使える環境であることを確認。COOP / COEP なしでは `SharedArrayBuffer` は使えない |

**設計ドキュメントの変更が必要な箇所はない。** 04 §2.1 の記述に「実測で拒否された」事実を追記する程度。

## 5. 次の検証

P1 と P4 に進む。物体検知モデルを ONNX Runtime Web と TensorFlow.js の両方で動かし、Worker 内での推論時間とバックエンドごとの差を測る。
